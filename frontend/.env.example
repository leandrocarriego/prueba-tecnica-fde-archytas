# Frontend environment — copy to frontend/.env.local (the devcontainer's postCreate does this).
# Base URL of the backend API. Inside a Codespace everything is localhost (Next calls it server-side).
NEXT_PUBLIC_API_URL=http://localhost:8000
