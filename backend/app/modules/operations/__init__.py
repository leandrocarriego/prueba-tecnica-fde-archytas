"""Operations: the system operating on itself.

Job runs, configurable business parameters and the health of the platform.
Everything here lives in the `operations` schema, apart from the extraction pipeline
(`raw` -> `staging` -> `core`) it watches over.
"""
